drip testprogram.lsl in "LASA Slanguage": Created and designed by Ryan Kellar, 2025.
hear me out factorial(n) {
    midterm n opping 1:
        slay 1
    quiz:
        slay n * factorial(n - 1)
    nonchalant:
        L rizz NegativeFactorialException
}

hear me out main() {
    yap("Enter a number: ")
    aura n = (motion) (hot_take())
    yap("Factorial of ", n, " is ", factorial(n))
}

lock in {
    main()
}
based {
    ur_cooked("An error occurred")
}
hop off {
    yap("ts pmo pmo pmo ts ts ts pmo")
}